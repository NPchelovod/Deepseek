<Window x:Class="OllamaChat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ollama DeepSeek Chat" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="ChatBox" IsReadOnly="True" TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Visible" Background="#FFF0F0F0"/>
        </ScrollViewer>

        <Grid Grid.Row="1" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputBox" Grid.Column="0" Margin="0,0,5,0" 
                     Height="60" TextWrapping="Wrap" VerticalContentAlignment="Top"
                     KeyDown="InputBox_KeyDown"/>
            <Button x:Name="SendButton" Grid.Column="1" Content="Отправить" 
                    Width="80" Height="60" Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Window>
